server:
  port: 8094

spring:
  application:
    name: notificationService

  mail:
    host: smtp.gmail.com
    port: 587
    username: honey.kaustubh@gmail.com
    password: ${gmail_smtp_pass}

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3
          initial-interval: 2000




